# usual keys
export OPENAI_API_KEY=...
export GOOGLE_KG_API_KEY=...
export TAVILY_API_KEY=...       

# input/output (or leave defaults)
export MDM_INPUT_XLSX=query_group2.xlsx
export MDM_OUT_JSON=mdm_results.json

# logging and enforcement
export MERGE_DEBUG=1             # see detailed merge logs (optional but helpful)
export DEBUG_RAW=1               # dump tool + LLM artifacts into ./debug_raw
unset MDM_MAX_ROWS               # process ALL rows
export PROVENANCE_ENFORCE=1      # keep this ON to drop unprovenanced fields

python3 searchFunction.py
